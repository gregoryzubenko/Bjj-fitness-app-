// Пример функции генерации тренировки по фазе
// Замените на вашу реальную логику!

export function generateWorkoutForPhase(phase, userData) {
  // Здесь можно использовать phase и userData для генерации плана
  return {
    phase,
    message: `Workout for phase: ${phase}`,
    // ...добавьте детали тренировки
  };
}
